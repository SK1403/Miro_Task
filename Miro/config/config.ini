[schema]

landingFileSchema = initiator_id StringType(),event StringType(),timestamp TimestampType(),channel StringType(),device_type StringType(),campaign StringType(),browser_version StringType()

[event_specification]

user_registeration = initiator_id,event,timestamp,channel
application_loading = initiator_id,event,timestamp,device_type

